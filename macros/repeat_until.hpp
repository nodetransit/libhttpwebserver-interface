#ifndef FB_HTTP_SERVER_MACRO_REPEAT_UNTIL_HPP__
#define FB_HTTP_SERVER_MACRO_REPEAT_UNTIL_HPP__

/** @brief repeat loop until condition is met */
#define repeat do
/** @brief repeat loop until condition is met */
#define until(condition) while(!(condition))

#endif /* FB_HTTP_SERVER_MACRO_REPEAT_UNTIL_HPP__ */
